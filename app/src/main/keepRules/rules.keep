# Gson reflects on the API DTOs and persisted User identity. Many server fields
# intentionally use their Kotlin names without @SerializedName; preserve those
# fields and constructors when optimizing the release APK.
-keep,allowoptimization class com.entrymyslotbe.app.network.model.** {
    <fields>;
    <init>(...);
}
